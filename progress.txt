# Ralph Progress Log
Started: Sun Mar  1 23:44:47 EST 2026

## Codebase Patterns
- TypeScript project: use `npm run typecheck` (tsc --noEmit) before committing
- Unit tests use Node.js built-in test runner: `npm test` runs `tsx --test tests/**/*.test.ts`
- Test files go in `tests/` directory with `.test.ts` suffix; included in tsconfig.json
- YAML frontmatter parser: `src/frontmatter-parser.ts` — import { parseNoteFile, parseNoteString, extractWikilinks }
- Vault note spelling: "lingelpedia" (gel, not gle) in tags like `lingelpedia/canonical` — easy to misspell as "linglepedia"
- `yaml` npm package parses YAML frontmatter; returns dates as Date objects, nulls for empty fields, arrays for lists
- Wikilinks `[[Target|Display]]` — parser extracts target only (not display alias)
- Run scripts with `npx tsx scripts/<name>.ts`
- IronClaw CLI: `ironclaw status` for health, `ironclaw config list` for settings, `ironclaw doctor` for diagnostics
- IronClaw stores secrets in macOS keychain (`secrets_master_key_source: keychain`), not in env vars
- IronClaw config is in `~/.ironclaw/.env` (database-backed, use `ironclaw config set` to modify)
- PostgreSQL for IronClaw runs via Homebrew on /tmp:5432 (not Docker)
- IronClaw MCP servers managed via `ironclaw mcp list` / `ironclaw mcp add`
- Always update AGENTS.md Environment Details table when infrastructure changes
- Docker Desktop settings at `~/Library/Group Containers/group.com.docker/settings.json`
- pmset changes require sudo — scripts that need it should document the sudo requirement clearly
- Docker `--restart unless-stopped` is a convention enforced in all `docker run` commands (not automated tooling)
- Neo4j 5.26.0 pinned (NOT latest 5.26.x) — GDS plugin has no compatible version for 5.26.21+
- Docker Desktop memory must be ≥16GB for Neo4j 8GB heap — settings.json `memoryMiB: 16384`
- Neo4j auth: `neo4j` / `lingelpedia2026` — container name: `linglepedia`
- Neo4j data persists at `$HOME/neo4j/data`, logs at `$HOME/neo4j/logs`
- Use `cypher-shell -u neo4j -p lingelpedia2026` inside the container for Cypher queries
- Pipe Cypher queries via stdin (`docker exec -i ... cypher-shell` with `input:`) — backticks in Cypher get mangled by shell when passed as args
- All Cypher DDL uses `IF NOT EXISTS` for idempotency — schema can be re-applied safely
- Reusable Cypher files live in `cypher/` directory
- MCP servers for IronClaw run as HTTP services — register with `ironclaw mcp add <name> <url>`
- mcp-neo4j-cypher uses SSE (text/event-stream) responses — parse with `data: ` prefix matching
- Use launchd LaunchAgents (~/Library/LaunchAgents/) for persistent MCP server processes with KeepAlive + RunAtLoad
- uvx caches Python packages — first run downloads, subsequent runs are fast
- IronClaw `mcp test <name>` verifies connectivity and lists available tools
- MCP SDK StreamableHTTPServerTransport stateless mode: create new server + transport per request, not shared
- Vault directory is `Linglepedia` (not `Lingelpedia`) at `~/Library/Mobile Documents/com~apple~CloudDocs/Linglepedia`
- Node.js `fs.watch({ recursive: true })` uses FSEvents natively on macOS — no extra library needed
- MCP servers on different ports: Neo4j=8765, vault=8766. Next server should use 8767+
- xAI has NO embedding models (confirmed 2026-03-02) — use Ollama nomic-embed-text (768 dims) as local fallback
- Ollama runs at http://localhost:11434 with OpenAI-compatible API at /v1/embeddings
- Embedding pipeline module: `src/embedding.ts` — import { generateEmbedding, storeClaimEmbedding, findSimilarClaims }
- Neo4j vector index dimensions MUST match the embedding model (currently 768 for nomic-embed-text)
- To change embedding dimensions: drop + recreate the vector index AND update cypher/lingelpedia-schema.cypher
- IronClaw identity system uses workspace memory files (IDENTITY.md, SOUL.md, AGENTS.md, USER.md, MEMORY.md), NOT YAML files
- `ironclaw memory write <path>` writes to workspace, `ironclaw memory read <path>` reads, `ironclaw memory tree` shows structure
- `ironclaw memory write` accepts content via stdin (pipe) or as second arg — use stdin for multi-line content
- Agent identity YAML files go in `agents/` dir (version controlled) — setup script writes them to IronClaw workspace
- SOUL.md (alignment principles) and USER.md (Mike's info) are shared across all agents — don't overwrite them per-agent
- Google OAuth for MCP servers requires interactive browser flow — `npm run setup-<name>-mcp` opens browser for consent
- Google OAuth credentials (client_id, client_secret) are at `~/Library/Application Support/gogcli/credentials.json`
- MCP server OAuth tokens stored in macOS Keychain (service: `vega-<service-name>`, account: `refresh_token`)
- IronClaw WASM tools use `ironclaw tool install <source-dir>` + `ironclaw tool auth <name>` — also requires browser
- `cargo-component` required for building IronClaw WASM tools — install with `cargo install cargo-component`
- Rust `wasm32-wasip2` target required — install with `rustup target add wasm32-wasip2`
- `gog` CLI (v0.11.0) at /opt/homebrew/bin/gog provides Google API access — tokens encrypted in file keyring (needs GOG_KEYRING_PASSWORD)
- MCP port allocation: Neo4j=8765, vault=8766, google-calendar=8767, gmail=8768, OAuth callback=8769, google-drive=8770. Next server should use 8771+.
- Gmail API uses base64url-encoded body parts — decode with `-` → `+`, `_` → `/` then Buffer.from(base64, "base64")
- Gmail search_emails: use `format: "metadata"` with `metadataHeaders` for efficient listing; `format: "full"` only for read_email
- Google Drive: export Google Docs via `files/<id>/export?mimeType=text/plain`, Sheets as CSV, use `alt=media` for raw download
- Drive sync state persists in `~/.vega/drive-sync-state.json` — tracks lastSyncTimestamp per folder ID
- Drive folder scoping uses parent-chain walking (file → parent → parent... → scoped folder) — may need optimization for deep hierarchies
- Entity mapper module: `src/entity-mapper.ts` — import { mapEntityToNeo4j, mapEntityNoteFile, mapEntityNoteString, generateEntityId, mapTruthScore }
- Truth score mapping: verified→0.95, agent-populated→0.7, stale→0.5 (flagged for reverification), conflicted→0.5 (creates OpenQuestion), unscored→0.5
- Entity IDs are filename slugs (lowercase, hyphens): `generateEntityId(filePath, legalName)` — file path takes priority
- Use MERGE on `{id: "..."}` for idempotent Neo4j node creation — never use CREATE for mapper nodes
- cypher-shell outputs booleans as `TRUE`/`FALSE` (uppercase) — use `.toUpperCase().includes("TRUE")` in assertions
- Ownership chains: `owned-by` field creates `(:Entity)-[:BELONGS_TO]->(:Entity)`, `parent-entity` also creates BELONGS_TO
- Wikilinks create `(:Entity)-[:RELATED_TO]->(:Entity)` edges, excluding entities already linked via BELONGS_TO
- Source nodes: every mapped entity gets a `(:Source)` node with `source_type: "obsidian_vault"` linked via `[:SOURCED_FROM]`
- When creating placeholder Entity nodes (from wikilinks), set `entity_type: "unknown"` and `truth_score: 0.5`
- Account mapper: `src/account-cashflow-mapper.ts` — import { mapAccountToNeo4j, mapCashFlowToNeo4j, mapAccountNoteFile, mapCashFlowNoteFile }
- Accounts map to (:Entity {entity_type: "account"}) — NOT a separate label, uses Entity with account_type property
- Cash Flows map to (:Entity {entity_type: "cash-flow"}) — same pattern: Entity label, distinguished by entity_type
- Institution references: set `entity_type: "institution"` on placeholder nodes (not "unknown") for better semantics
- Account relationship types: held_at (institution), joint_holder, lender — all via RELATED_TO with `r.type` property
- Real vault has 39 Account notes but 0 standalone Cash Flow notes (investment cash flows are embedded in Deal notes)
- `original-loan-amount` and `original-balance` are both used in vault notes — mapper handles both field names
- Investment Deal mapper: `src/investment-person-mapper.ts` — import { mapInvestmentDealToNeo4j, mapInvestmentDealNoteFile, generateBetId }
- Investment Position mapper: same module — import { mapInvestmentPositionToNeo4j, mapInvestmentPositionNoteFile, generatePositionId }
- Person mapper: same module — import { mapPersonToNeo4j, mapPersonNoteFile, generatePersonId }
- Investment Deals map to (:Bet {bet_type: "intentional"}) — NOT Entity label, uses the Bet label per schema
- Investment Positions map to (:Entity {entity_type: "investment-position"}) — linked to Bet via STAKED_ON
- Persons map to (:Entity {entity_type: "person"}) — family fields (spouse/parent/child/sibling) use RELATED_TO with type property
- Deal notes in `WE/Deals/` (36 notes), Position notes in `Finance/Positions/` (39 notes), Person notes in `People/` (178 notes)
- Bet IDs use "bet-" prefix, Position IDs use "pos-" prefix, Person IDs use filename slug (no prefix)
- Position `deal` field links to Bet via STAKED_ON; empty `deal` (investment-origin: direct) means no deal backlink
- Person `financial-roles` is an array of objects with `role` and `entity` keys — parse as Record<string, unknown>
- Vehicle entities (from deal `vehicle` field) get placeholder entity_type "vehicle" (not "unknown")
- Custodian entities get placeholder entity_type "institution"
- Generic mapper: `src/generic-mapper.ts` — handles institution, vehicle, property, vendor template types → Entity nodes
- Migration script: `scripts/run-migration.ts` — discovers and processes all schema-driven notes. Run: `npm run run-migration`. Dry run: `npm run run-migration -- --dry-run`
- Vault directories for structured migration: Finance/{Entities,Accounts,Institutions,Positions}, Auto/, Properties/{Current,Previous,Prospective,Vendors}
- Skip in migration: README.md, MOC files (end with " MOC.md"), _schemas/, dashboard type, files without `type` frontmatter
- Property notes have `type: property`, Vehicle notes have `type: vehicle`, Institution notes have `type: institution`, Vendor notes have `type: vendor`
- Properties/Vendors/Telescope Properties LLC.md has `type: entity` (not vendor) — the migration routes it to the entity mapper
- Vault paths contain spaces ("Mobile Documents") — use double-quoted strings or escaped spaces, NOT `$HOME` in string interpolation with subshell
- Shell quoting for vault paths: escape `Mobile\ Documents` or use double quotes. `$VAULT` var assignment breaks if spaces not handled
- Claim decomposition engine: `src/claim-decomposition.ts` — import { decomposeNote, decomposeNoteFile, decomposeNoteString, chunkText, parseLLMResponse }
- LLM backend: Ollama qwen3:32b locally (no xAI API key in env). Set XAI_API_KEY to switch to Grok-4.20.
- qwen3:32b uses thinking mode by default — add `/no_think` to system prompt for faster JSON-only responses
- Chunk threshold 2500 tokens (~10000 chars) for local models — larger notes get split into 2000-token segments with 200-token overlap
- LLM fetch timeout: 10 min per request, 2 retries — local 32B models are slow (~200-400s per 2K-token chunk on M1 Max)
- Claims stored as (:Claim) with SOURCED_FROM → (:Source) and ABOUT → (:Entity). Truth basis "agent-populated", truth_score 0.7
- Claim IDs use `claim-<filename-slug>-<index>` pattern — deterministic for idempotency
- Claim deduplication: generate embeddings, compare cosine similarity, >0.95 = duplicate. Keep first occurrence.
- `node:test` `it()` function signature: `it(name, options, fn)` — NOT `it(name, fn, options)`
- `require()` doesn't work in ESM modules (type: "module") — use top-level `import` or dynamic `await import()`
- Generic mapper test `creates RELATED_TO for loan-account` is a pre-existing failure (not related to US-017)

---

## 2026-03-02 - US-001
- **What was implemented:** Environment health-check diagnostic script + AGENTS.md environment verification
- **Files changed:**
  - `scripts/health-check.ts` — new diagnostic script checking Docker, IronClaw, PostgreSQL, xAI API, pmset
  - `package.json` — new TypeScript project setup with tsx, tsc
  - `tsconfig.json` — strict TypeScript config
  - `.gitignore` — added node_modules/ and dist/
  - `AGENTS.md` — updated Environment Details with verified component statuses and host details
  - `prd.json` — US-001 passes: true
- **Learnings for future iterations:**
  - IronClaw default model was `grok-4-1-fast-reasoning`, updated to `grok-4.20` per PRD via `ironclaw config set selected_model grok-4.20`
  - xAI API key is NOT in shell environment — it's in IronClaw's keychain. Direct curl tests need the key exported; IronClaw handles auth internally.
  - `ironclaw -m "..."` single-message mode exists but can be slow/hang — use `ironclaw doctor` for quick connectivity checks instead
  - pmset sleep is currently not disabled (sleep=1, disksleep=10 on AC) — US-002 will fix this
  - IronClaw has 1 MCP server configured (imessage on port 8892) — Neo4j, filesystem, Google APIs still to be added
  - PostgreSQL is Homebrew-managed, not Docker — important distinction for container management stories
---

## 2026-03-02 - US-002
- **What was implemented:** Always-on configuration script + Docker autoStart + restart policy convention
- **Files changed:**
  - `scripts/configure-always-on.ts` — new script that configures pmset (sleep=0, disksleep=0), Docker autoStart, and documents restart policy
  - `package.json` — added `configure-always-on` npm script
  - `AGENTS.md` — added Always-On Configuration section, updated host details, added to completed stories
  - `prd.json` — US-002 passes: true
- **Learnings for future iterations:**
  - Docker Desktop settings are in `~/Library/Group Containers/group.com.docker/settings.json` — can be modified directly as JSON
  - `autoStart: true` in Docker settings makes Docker start on login (no launchd plist needed for Docker Desktop)
  - pmset requires sudo — can't be applied from non-interactive sessions like Claude Code. Document sudo commands clearly.
  - Docker has launchd plists at `/Library/LaunchDaemons/com.docker.{socket,vmnetd}.plist` for its daemon infrastructure
  - The `--restart unless-stopped` policy is a convention, not an automated check — each `docker run` in subsequent stories must include it
---

## 2026-03-02 - US-003
- **What was implemented:** Neo4j 5.26.0 community in Docker with APOC + GDS plugins, 8GB heap, persistent volumes
- **Files changed:**
  - `scripts/setup-neo4j.ts` — new setup script: pulls image, creates dirs, starts container, verifies health
  - `package.json` — added `setup-neo4j` npm script
  - `AGENTS.md` — updated Neo4j row to verified, added Neo4j Configuration section, added to completed stories
  - `prd.json` — US-003 passes: true
- **Learnings for future iterations:**
  - Neo4j `5.26-community` tag resolves to 5.26.21 which has NO GDS plugin support — must pin to `5.26.0-community`
  - Docker Desktop default memory is 8092 MiB — not enough for Neo4j 8GB heap (exit code 137 = OOM kill). Increased to 16384 MiB.
  - Docker Desktop must be fully restarted after settings.json memory change — `osascript -e 'quit app "Docker"'` then `open -a Docker`
  - Docker Desktop VM resize can take 60-120s — the first restart sometimes hangs; kill stale processes with `killall Docker` before retrying
  - NEO4J_PLUGINS env var accepts JSON array for auto-install: `'["apoc", "graph-data-science"]'`
  - APOC is bundled in `/var/lib/neo4j/labs/` inside the image; GDS is fetched from `graphdatascience.ninja/versions.json`
  - `cypher-shell` inside container is the best way to verify connectivity — `docker exec linglepedia cypher-shell -u neo4j -p <pw> "RETURN 1"`
  - Neo4j Browser at http://localhost:7474 returns HTTP 200 when ready
---

## 2026-03-02 - US-004
- **What was implemented:** Lingelpedia Neo4j schema — 5 uniqueness constraints, 1 vector index, 1 full-text index, 6 property lookup indexes
- **Files changed:**
  - `cypher/lingelpedia-schema.cypher` — reusable, idempotent schema DDL (all `IF NOT EXISTS`)
  - `scripts/apply-schema.ts` — TypeScript script that applies schema via cypher-shell and verifies all constraints/indexes
  - `package.json` — added `apply-schema` npm script
  - `AGENTS.md` — added US-004 to completed stories
  - `prd.json` — US-004 passes: true
- **Learnings for future iterations:**
  - Backticks in Cypher (e.g. `` `vector.dimensions` ``) get interpreted as shell command substitution when passed via `docker exec ... "query"` — use `docker exec -i` with stdin pipe instead
  - Neo4j `SHOW CONSTRAINTS` and `SHOW INDEXES` return CSV-formatted output from cypher-shell — parse by splitting on commas (watch for quoted values)
  - Vector index type shows as "VECTOR", full-text as "FULLTEXT", property indexes as "RANGE" in `SHOW INDEXES` output
  - Uniqueness constraints implicitly create RANGE indexes on the constrained property — no need for separate `CREATE INDEX` on id fields
  - Schema is fully idempotent — tested by running `npm run apply-schema` twice with identical results
---

## 2026-03-02 - US-005
- **What was implemented:** Neo4j MCP server (mcp-neo4j-cypher) installed, configured as persistent launchd service, registered with IronClaw
- **Files changed:**
  - `scripts/setup-neo4j-mcp.ts` — new setup script: installs mcp-neo4j-cypher via uvx, creates launchd plist, starts server, registers with IronClaw, verifies read/write
  - `package.json` — added `setup-neo4j-mcp` npm script
  - `AGENTS.md` — added Neo4j MCP row to Environment Details, added Neo4j MCP Server config section, updated IronClaw MCP count, added to completed stories
  - `prd.json` — US-005 passes: true
- **Learnings for future iterations:**
  - IronClaw MCP servers are HTTP-based — the server URL is registered with `ironclaw mcp add <name> <url>`
  - mcp-neo4j-cypher uses SSE (Server-Sent Events) transport — responses come as `text/event-stream` with `data: {json}` lines
  - The `uvx` tool (from uv) caches packages in `~/.cache/uv/` — first run downloads dependencies, subsequent runs are near-instant
  - launchd LaunchAgents at `~/Library/LaunchAgents/` are the correct way to persist user-level services on macOS
  - KeepAlive + RunAtLoad in the plist ensures the MCP server survives crashes and starts on login
  - mcp-neo4j-cypher requires the APOC plugin installed in Neo4j for schema inspection (already installed in US-003)
  - The MCP server exposes 3 tools: `get_neo4j_schema` (read-only), `read_neo4j_cypher` (read-only), `write_neo4j_cypher` (destructive)
  - `ironclaw mcp test <name>` is a useful quick-check that verifies connectivity and lists available tools
  - Port 8765 used for Neo4j MCP — future MCP servers should use different ports to avoid conflicts
---

## 2026-03-02 - US-006
- **What was implemented:** Custom vault filesystem MCP server connecting IronClaw to the Lingelpedia Obsidian vault with FSEvents file watching
- **Files changed:**
  - `src/mcp-vault-server.ts` — custom MCP server: 5 tools (list_directory, read_file, write_file, search_files, get_changed_files), FSEvents watcher, 60s polling fallback
  - `scripts/setup-vault-mcp.ts` — setup script: creates launchd plist, starts server, tests read/write/watch, registers with IronClaw
  - `package.json` — added deps (@modelcontextprotocol/sdk, express, zod), added setup-vault-mcp and vault-server scripts
  - `AGENTS.md` — added vault MCP to Environment Details, documented Vault Filesystem MCP Server section, updated MCP count
  - `prd.json` — US-006 passes: true
- **Learnings for future iterations:**
  - Vault directory name is `Linglepedia` (with 'e' in the middle), NOT `Lingelpedia` — at `~/Library/Mobile Documents/com~apple~CloudDocs/Linglepedia`
  - The MCP SDK's `StreamableHTTPServerTransport` in stateless mode requires a NEW server+transport instance per HTTP request — shared instances cause "Already connected to a transport" errors
  - `Protocol.connect()` throws if `_transport` is already set — must call `close()` first or use per-request instances
  - The MCP SDK in stateless mode does NOT enforce initialization for non-init requests (no session validation, no protocol version check required)
  - Node.js `fs.watch({ recursive: true })` uses FSEvents natively on macOS — provides near-instant file change detection without extra dependencies
  - The Obsidian vault is on iCloud (`Mobile Documents/com~apple~CloudDocs/`) — paths contain spaces; handle with care in shell commands
  - Write access is intentionally scoped to `_agent_insights/` only — future stories should use the `write_file` tool for agent output
  - Port allocation: Neo4j MCP = 8765, vault MCP = 8766 — future MCP servers should increment from 8767
  - Added express, @modelcontextprotocol/sdk, and zod as project dependencies — available for future custom MCP servers
---

## 2026-03-02 - US-007
- **What was implemented:** Embedding generation pipeline using Ollama (local) with nomic-embed-text (768 dimensions), Neo4j vector storage, and similarity search
- **Files changed:**
  - `src/embedding.ts` — new embedding pipeline module: generateEmbedding, generateEmbeddings, storeClaimEmbedding, findSimilarClaims (OpenAI-compatible API, configurable backend)
  - `scripts/test-embedding.ts` — comprehensive test script: single/batch embedding, latency, Neo4j storage, similarity search, semantic ranking
  - `cypher/lingelpedia-schema.cypher` — vector index updated from 1536→768 dimensions (nomic-embed-text)
  - `package.json` — added `test-embedding` npm script
  - `AGENTS.md` — added Embedding Pipeline section, updated vector index dims, added to Environment Details and Completed Stories
  - `prd.json` — US-007 passes: true
- **Learnings for future iterations:**
  - xAI has NO embedding models (confirmed 2026-03-02 by listing /v1/models) — the /v1/embeddings endpoint exists but returns 404 for all model names
  - Ollama's nomic-embed-text is 768 dimensions, NOT 1536 — the vector index must match the model's output
  - To change embedding models: drop the vector index (`DROP INDEX claim_embeddings IF EXISTS`), recreate with new dimensions, update `cypher/lingelpedia-schema.cypher`
  - Ollama's OpenAI-compatible API at /v1/embeddings works identically to OpenAI — same request/response format, no auth required
  - nomic-embed-text latency: ~20ms per claim locally on M1 Max — far under the 500ms threshold
  - The embedding pipeline is backend-agnostic: set EMBEDDING_BASE_URL + EMBEDDING_API_KEY to switch providers (OpenAI, xAI when available, etc.)
  - IronClaw's encrypted secrets (AES in PostgreSQL) cannot be easily decrypted outside of IronClaw — don't try to extract API keys from the DB
  - The old xAI API key in `~/.ironclaw/settings.toml` is expired — current key is only in the encrypted secrets table
---

## 2026-03-02 - US-008
- **What was implemented:** Lingelpedia Agent identity file (YAML canonical definition) + setup script that writes identity to IronClaw workspace memory
- **Files changed:**
  - `agents/lingelpedia.yaml` — canonical agent identity definition with all required sections (personality, thinking model, ownership, north star, trust level, operating algorithm, charter principles, key behaviors, deletion policy, tools, startup instructions)
  - `scripts/setup-agent.ts` — setup script: reads YAML, validates structure, writes IDENTITY.md + AGENTS.md to IronClaw workspace, verifies content, tests agent instantiation
  - `package.json` — added `setup-agent` npm script
  - `AGENTS.md` — added Lingelpedia Agent Identity section, updated Environment Details table, added to Completed Stories
  - `prd.json` — US-008 passes: true
- **Learnings for future iterations:**
  - IronClaw does NOT use YAML identity files natively — it uses workspace memory markdown files (IDENTITY.md, SOUL.md, AGENTS.md, USER.md, MEMORY.md)
  - `ironclaw memory write <path>` writes to workspace memory, accepts content via stdin for multi-line content
  - `ironclaw memory tree` shows the full workspace structure — useful for debugging what files the agent sees
  - SOUL.md contains the 7 charter alignment principles and USER.md contains Mike's info — these are shared across all agent identities, don't overwrite
  - The workspace README.md explains: IDENTITY.md = name/vibe/personality, SOUL.md = values/boundaries, AGENTS.md = session routine
  - Agent reads workspace files at session start — changes to IDENTITY.md take effect on next `ironclaw run`
  - grok-4.20 model returned "Model not found" from xAI API during testing (2026-03-02) — may be intermittent or model name changed
  - The `ironclaw -m "message" --no-onboard` flag sends a single message and exits — useful for quick tests but model must be available
  - Version-control the canonical YAML in `agents/` dir, then use setup script to deploy to IronClaw workspace — keeps identity portable and auditable
---

## 2026-03-02 - US-009
- **What was implemented:** Google Calendar MCP server with OAuth 2.0, IronClaw WASM tool installation, LaunchAgent persistence, IronClaw registration
- **Files changed:**
  - `src/mcp-google-calendar-server.ts` — custom MCP server: 2 tools (list_events, get_event), OAuth token refresh from Keychain, read-only calendar access
  - `scripts/setup-google-calendar-mcp.ts` — setup script: OAuth browser flow with local callback server, Keychain token storage, LaunchAgent, IronClaw registration, verification
  - `package.json` — added `setup-google-calendar-mcp` and `google-calendar-server` npm scripts
  - `AGENTS.md` — added Google Calendar MCP to Environment Details, added server config section, updated IronClaw MCP count, added to Completed Stories
  - `prd.json` — US-009 passes: true
- **Architecture decisions:**
  - Dual approach: custom TypeScript MCP server (HTTP, port 8767) AND IronClaw native WASM tool (google-calendar-tool)
  - Custom MCP server handles OAuth independently (stores refresh token in macOS Keychain), while WASM tool uses IronClaw's internal auth
  - Read-only enforced at OAuth scope level (calendar.events.readonly) — the agent sees list_events and get_event only
  - Token refresh handled automatically — access tokens cached with 60s buffer before expiry
- **Learnings for future iterations:**
  - Google OAuth requires interactive browser flow — no way to fully automate from non-interactive sessions
  - The `ironclaw tool auth` command also requires browser interaction (opens Google consent page)
  - `ironclaw tool install` needs `cargo-component` (Rust) + `wasm32-wasip2` target — install these first
  - Google OAuth credentials are shared at `~/Library/Application Support/gogcli/credentials.json` (from `gog` CLI)
  - `gog` CLI (v0.11.0) at /opt/homebrew/bin/gog has existing Google tokens but they're encrypted in a file keyring requiring GOG_KEYRING_PASSWORD
  - For MCP servers that need OAuth, use the pattern: local HTTP callback server → capture auth code → exchange for tokens → store refresh_token in Keychain
  - Keychain entries: `security add-generic-password -s <service> -a <account> -w <value> -U` (U = update if exists)
  - The OAuth callback port (8769) is separate from the MCP server port (8767) — callback only runs during setup
  - IronClaw WASM tools and MCP servers are complementary — WASM is native integration, MCP is HTTP protocol. Both can coexist.
  - For Gmail (US-010) and Drive (US-011): follow this same dual pattern (custom MCP server + WASM tool)
---

## 2026-03-02 - US-010
- **What was implemented:** Gmail MCP server with OAuth 2.0 (gmail.readonly scope), LaunchAgent persistence, IronClaw registration
- **Files changed:**
  - `src/mcp-gmail-server.ts` — custom MCP server: 2 tools (search_emails, read_email), OAuth token refresh from Keychain, read-only Gmail access, base64url body decoding, multipart MIME handling
  - `scripts/setup-gmail-mcp.ts` — setup script: OAuth browser flow with local callback server, Keychain token storage, LaunchAgent, IronClaw registration, verification
  - `package.json` — added `setup-gmail-mcp` and `gmail-server` npm scripts
  - `AGENTS.md` — added Gmail MCP to Environment Details, added server config section, updated IronClaw MCP count, added to Completed Stories
  - `prd.json` — US-010 passes: true
- **Architecture decisions:**
  - Same pattern as Google Calendar MCP: custom TypeScript MCP server on HTTP port 8768
  - search_emails uses Gmail search syntax (same as Gmail search box) — very powerful query language
  - read_email fetches full message with `format: "full"`, extracts text/plain preferring over text/html
  - Email bodies truncated at 10,000 chars to avoid overwhelming agent context windows
  - Gmail base64url encoding handled with Buffer-based decoding (no external deps)
  - Multipart MIME recursion handles nested multipart/alternative and multipart/mixed
- **Learnings for future iterations:**
  - Gmail API uses base64url encoding (not standard base64) — replace `-` with `+` and `_` with `/` before decoding
  - Gmail `messages.list` only returns IDs — need a second `messages.get` call per message for metadata
  - Use `format: "metadata"` with `metadataHeaders` param for efficient listing (avoids downloading full body)
  - Use `format: "full"` only when reading a specific message (includes full payload with body parts)
  - Gmail multipart messages can be deeply nested (multipart/mixed → multipart/alternative → text/plain)
  - Prefer text/plain over text/html for body extraction — strip HTML tags as fallback
  - OAuth callback port 8769 is shared with Calendar setup — only one OAuth flow can run at a time
  - Port 8768 allocated for Gmail MCP — next server (Google Drive) should use 8770
---

## 2026-03-02 - US-011
- **What was implemented:** Google Drive MCP server with OAuth 2.0 (drive.readonly scope), folder scoping (GIX, WE, Finance), sliding window chunking, incremental sync via lastSyncTimestamp, LaunchAgent persistence, IronClaw registration
- **Files changed:**
  - `src/mcp-google-drive-server.ts` — custom MCP server: 3 tools (list_files, read_document, get_sync_status), OAuth token refresh from Keychain, folder scoping with parent-chain verification, Google Docs/Sheets/PDF content extraction, sliding window chunking (2000-token segments, 200-token overlap), incremental sync timestamps
  - `scripts/setup-google-drive-mcp.ts` — setup script: OAuth browser flow with local callback server, Keychain token storage, LaunchAgent, IronClaw registration, scoped folder verification
  - `package.json` — added `setup-google-drive-mcp` and `google-drive-server` npm scripts
  - `AGENTS.md` — added Google Drive MCP to Environment Details, added server config section, updated IronClaw MCP count, added to Completed Stories
  - `prd.json` — US-011 passes: true
- **Architecture decisions:**
  - Same pattern as Gmail/Calendar: custom TypeScript MCP server on HTTP port 8770
  - Folder scoping enforced at two levels: list_files only queries within scoped folder IDs, read_document walks parent chain to verify file is under a scoped folder
  - Google Docs exported as plain text, Sheets as CSV, PDFs as best-effort text extraction (no OCR)
  - Sliding window chunking: ~2000-token segments (8000 chars), ~200-token overlap (800 chars)
  - Sync state stored in `~/.vega/drive-sync-state.json` — simple JSON with folderId → ISO timestamp mapping
  - Subfolder navigation supported via path splitting (e.g., "Meeting Notes/2026")
- **Learnings for future iterations:**
  - Google Drive API v3 uses `files.export` for Google Workspace documents (Docs, Sheets, Slides) and `files?alt=media` for binary/text files
  - Google Docs → `mimeType=text/plain` export gives clean text without HTML artifacts
  - Google Sheets → `mimeType=text/csv` export works for single-sheet; multi-sheet exports only get the first sheet
  - PDF text extraction from Drive is limited without OCR — the raw binary content has some readable text but not reliably structured
  - Folder scoping by parent-chain walking requires one API call per ancestor — for deeply nested files this could be slow. Consider caching folder ancestry.
  - `drive.readonly` scope grants access to ALL files the user can see, not just specific folders — scoping is enforced in application code
  - OAuth callback port 8769 is shared across Calendar, Gmail, and Drive setups — only one can run at a time
  - Port 8770 allocated for Google Drive MCP — next server should use 8771+
  - Sync state file needs the `~/.vega/` directory created on first use — `mkdir -p` handles this
---

## 2026-03-02 - US-012
- **What was implemented:** YAML frontmatter parser for Obsidian markdown files, handling all 7 template types (Entity, Account, Investment Deal/Position, Cash Flow, Person, Institution, Vehicle) with wikilink extraction and comprehensive unit tests
- **Files changed:**
  - `src/frontmatter-parser.ts` — new parser module: parseNoteFile, parseNoteString, extractWikilinks, LingelpediaFields types, wikilink extraction from frontmatter + body
  - `tests/frontmatter-parser.test.ts` — 64 unit tests covering all template types, edge cases (no frontmatter, empty file, malformed YAML, null values, CRLF, unknown types)
  - `package.json` — added `yaml` dependency, added `test` npm script
  - `tsconfig.json` — added `tests/**/*.ts` to include
  - `prd.json` — US-012 passes: true
- **Learnings for future iterations:**
  - "lingelpedia" (gel) vs "linglepedia" (gle) — extremely easy to misspell. Vault directory is "Linglepedia" but tags use "lingelpedia/canonical". Always double-check spelling.
  - The `yaml` npm package parses YAML dates (e.g., `2026-03-01`) into JavaScript Date objects, not strings — downstream code should handle both
  - The `yaml` package handles null/empty YAML values as JavaScript `null`, empty arrays as `[]`, empty strings as `""`
  - Node.js v25+ has a robust built-in test runner (`node:test`) — no need for Jest/Vitest. Use `npx tsx --test tests/**/*.test.ts`.
  - TypeScript `noUnusedLocals` is strict — importing a type you don't use fails typecheck. Use `import type { X }` or don't import.
  - Wikilinks can appear in both frontmatter string values AND body text — the parser extracts from both and deduplicates into `allWikilinks`
  - The regex `WIKILINK_RE` handles aliased links `[[Target|Display]]` — extracts only the target (first capture group before `|`)
  - Template type is determined by the `type` frontmatter field; investment sub-type by the `perspective` field (deal vs personal)
---

## 2026-03-02 - US-013
- **What was implemented:** Entity frontmatter-to-Neo4j mapper — maps Entity notes from Obsidian vault to Neo4j Entity nodes with properties, ownership chains (BELONGS_TO), wikilink relationships (RELATED_TO), Source nodes (SOURCED_FROM), truth score mapping, and OpenQuestion creation for conflicted scores
- **Files changed:**
  - `src/entity-mapper.ts` — new mapper module: mapEntityToNeo4j, mapEntityNoteFile, mapEntityNoteString, generateEntityId, generateSourceId, extractWikilinkTarget, mapTruthScore, escCypher, runCypher. Handles all Entity frontmatter fields including trust-specific properties.
  - `tests/entity-mapper.test.ts` — 34 tests (26 unit + 8 integration requiring Neo4j): ID generation, truth score mapping, Cypher escaping, wikilink extraction, full mapping with BELONGS_TO/RELATED_TO/SOURCED_FROM/OpenQuestion verification, idempotency test
  - `scripts/test-entity-mapper.ts` — integration test script that maps a real vault Entity note and verifies in Neo4j Browser
  - `package.json` — added `test-entity-mapper` npm script
  - `prd.json` — US-013 passes: true
- **Learnings for future iterations:**
  - cypher-shell outputs booleans as `TRUE`/`FALSE` (uppercase) — assertions comparing output strings must use `.toUpperCase()` or case-insensitive matching
  - Use MERGE (not CREATE) for all mapper node operations to ensure idempotency — running the mapper twice on the same note should produce count=1
  - Entity IDs are deterministic slugs from filenames: `basename(filePath, ".md").toLowerCase().replace(/[^a-z0-9]+/g, "-")` — consistent across runs
  - Placeholder Entity nodes (created from wikilinks for entities not yet mapped) use `entity_type: "unknown"` — later mappers should MERGE to update these
  - The `owned-by` field contains strings like `"[[Greg Wasson]] (grantor)"` — need `extractWikilinkTarget()` to strip surrounding text
  - Trust-specific fields (trust-date, trust-state, irrevocable-type, distribution-rules) are stored as additional properties on the Entity node
  - The `parent-entity` frontmatter field also creates BELONGS_TO relationships, separate from the `owned-by` array
  - Wikilinks that appear in both `owned-by` and body text should only create BELONGS_TO (not duplicate RELATED_TO) — the mapper tracks BELONGS_TO targets to avoid this
  - YAML date values come through as JavaScript Date objects — use `.toISOString().split("T")[0]` to get `YYYY-MM-DD` strings for Neo4j
  - 7 real Entity notes in Finance/Entities/: trusts (revocable, irrevocable, special needs), LLCs (Tiger93, Telescope Properties), and a GST trust
  - TypeScript `noUnusedLocals` catches unused imports — be careful with `import { mock } from "node:test"` if you don't end up using it
---

## 2026-03-02 - US-014
- **What was implemented:** Account and Cash Flow frontmatter-to-Neo4j mappers — maps Account notes to (:Entity {entity_type: "account"}) nodes with institution links (RELATED_TO {held_at}), held-by (BELONGS_TO), joint-holders (RELATED_TO {joint_holder}), lender (RELATED_TO {lender}), Plaid connection status, and debt-specific fields. Cash Flow mapper creates (:Entity {entity_type: "cash-flow"}) nodes with from/to-account and from/to-entity links, amount, frequency, direction, tax-deductible, essential properties.
- **Files changed:**
  - `src/account-cashflow-mapper.ts` — new mapper module: mapAccountToNeo4j, mapCashFlowToNeo4j, mapAccountNoteFile, mapCashFlowNoteFile, mapAccountNoteString, mapCashFlowNoteString, generateAccountId, generateCashFlowId. Reuses runCypher, escCypher, mapTruthScore, generateEntityId, generateSourceId, extractWikilinkTarget from entity-mapper.ts.
  - `tests/account-cashflow-mapper.test.ts` — 25 tests (7 unit + 11 account integration + 7 cash flow integration): ID generation, type rejection, entity creation, institution links, held-by BELONGS_TO, joint-holders, debt fields, Source/SOURCED_FROM, lender, OpenQuestion for conflicted, idempotency, stale flagging, wikilinks, cash flow with from/to account/entity, linked-debt/investment, optional fields
  - `scripts/test-account-cashflow-mapper.ts` — integration test script: maps real vault Account notes and verifies in Neo4j
  - `package.json` — added `test-account-cashflow-mapper` npm script
  - `AGENTS.md` — added US-012, US-013, US-014 to completed stories
  - `prd.json` — US-014 passes: true
- **Learnings for future iterations:**
  - 39 real Account notes in vault (Finance/Accounts/): checking, savings, brokerage, mortgage, loan, crypto, credit-card, money-market, hsa, 529, retirement
  - 0 standalone Cash Flow notes exist — investment cash flows are embedded in WE/Deals/ Deal notes as markdown tables, not as standalone frontmatter notes
  - Account notes have variable field sets: basic accounts (checking/savings) have ~15 fields, debt accounts (mortgage/loan) have 25+ fields including principal-balance, interest-rate, collateral, etc.
  - Both `original-loan-amount` (mortgages) and `original-balance` (schema) are used — mapper handles both field names
  - Some accounts have extra custom fields (wealthfront-id, account-id, advisory-fee, tod, risk-profile, gdrive-folder, property) — these are not mapped but don't cause errors
  - Institution references should set placeholder entity_type to "institution" (not "unknown") for better graph semantics
  - Account IDs use the same slugification as Entity IDs — filenames like "Chase - Mike Checking 1622" become "chase-mike-checking-1622" (consecutive special chars collapse to single hyphen)
  - Reusing exports from entity-mapper.ts (runCypher, escCypher, etc.) avoids duplication — this pattern should continue for US-015
  - Cash Flow schema defines `linked-debt` and `linked-investment` fields — these create RELATED_TO edges to the referenced account/investment nodes
---

## 2026-03-02 - US-015
- **What was implemented:** Investment (Deal + Position) and Person frontmatter-to-Neo4j mappers. Investment Deal notes map to (:Bet {bet_type: "intentional"}) nodes with deal-sponsor, vehicle, vintage year, projected IRR/MOIC, hold period, exit strategy, and source system. Investment Position notes map to (:Entity {entity_type: "investment-position"}) nodes linked to the deal's Bet node via STAKED_ON, with held-by (BELONGS_TO), held-alongside, k1-entity, custodian (RELATED_TO). Direct investment positions (investment-origin: direct) support ticker, exchange, shares, restriction status, lock-up-end. Person notes map to (:Entity {entity_type: "person"}) nodes with relationship circle, contact frequency, aliases, company (works_at), family relationships (spouse/parent/child/sibling via RELATED_TO with type), financial-roles, trusts (trust_beneficiary), CPA/estate-attorney/wealth-managers as institution entities.
- **Files changed:**
  - `src/investment-person-mapper.ts` — new mapper module: mapInvestmentDealToNeo4j, mapInvestmentPositionToNeo4j, mapPersonToNeo4j, generateBetId, generatePositionId, generatePersonId, plus NoteFile/NoteString convenience functions for all three types. Reuses runCypher, escCypher, mapTruthScore, generateEntityId, generateSourceId, extractWikilinkTarget from entity-mapper.ts.
  - `tests/investment-person-mapper.test.ts` — 30 tests (10 unit + 3 deal integration + 4 position integration + 7 person integration + 6 type rejection): ID generation, Bet node creation, deal-sponsor/vehicle links, STAKED_ON to Bet, held-by BELONGS_TO, direct position support, family relationships, financial-roles, trusts, advisor links, idempotency, OpenQuestion for conflicted, stale flagging
  - `scripts/test-investment-person-mapper.ts` — integration test script: maps real vault Deal, Position, and Person notes and verifies in Neo4j
  - `package.json` — added `test-investment-person-mapper` npm script
  - `AGENTS.md` — added US-015 to completed stories
  - `prd.json` — US-015 passes: true
- **Learnings for future iterations:**
  - Investment Deal notes live in `WE/Deals/` (36 notes), Position notes in `Finance/Positions/` (39 notes), Person notes in `People/` (178 notes — most linked type)
  - Deal notes and Position notes are paired: every WE deal has one Deal note (company-level) and one or more Position notes (household-level)
  - Position notes link to Deal via `deal` wikilink field; direct investments (investment-origin: direct) have no deal backlink (e.g., PureCycle Technologies)
  - Direct positions have extra fields not in WE positions: ticker, exchange, shares, restriction-status, restriction-code, restriction-lift-date, custodian, share-price
  - Person notes have the most complex relationship structure: family (spouse/parent/child/sibling), financial-roles (array of objects with role+entity), trusts (array), advisors (cpa/estate-attorney/wealth-managers)
  - `financial-roles` is an array of objects `{role: string, entity: "[[Entity]]"}` — needs to be parsed as Record<string, unknown>, not string
  - Person family members should be created as placeholder entity_type "person" (not "unknown") for better semantics
  - Vehicle entities (from deal `vehicle` field) and custodian entities should be "vehicle" and "institution" respectively
  - The `toCypherBool` helper from account-cashflow-mapper.ts is not needed in investment-person-mapper — no boolean fields in these templates
  - TypeScript `noUnusedLocals` catches declared-but-unused functions — delete them rather than renaming with underscore prefix (TS doesn't honor the convention)
  - All 158 tests pass (30 new + 128 existing). Integration test: `npm run test-investment-person-mapper` maps 5 real notes from each category with 100% success rate.
---

## 2026-03-02 - US-016
- **What was implemented:** Full structured migration of all schema-driven notes in Finance/, Auto/, Properties/ to Neo4j. Created generic mapper for previously unhandled template types (institution, vehicle, property, vendor). Built migration script that discovers, routes, and processes all notes through appropriate mappers.
- **Files changed:**
  - `src/generic-mapper.ts` — new generic mapper: handles institution, vehicle, property, vendor template types → Entity nodes. Type-specific relationship field definitions, scalar property mappings, wikilink cross-references, BELONGS_TO/RELATED_TO, Source/SOURCED_FROM, OpenQuestion for conflicted scores.
  - `scripts/run-migration.ts` — migration script: discovers notes across 10 vault subdirectories, parses frontmatter for template type routing, processes through appropriate mapper (entity/account/cash-flow/investment-deal/investment-position/person/generic), comprehensive logging, Neo4j verification, dry-run mode.
  - `tests/generic-mapper.test.ts` — 24 tests: 4 unit (type rejection) + 20 integration (institution/vehicle/property/vendor node creation, relationships, idempotency, OpenQuestion, cleanup).
  - `package.json` — added `run-migration` npm script
  - `AGENTS.md` — added US-016 to completed stories
  - `prd.json` — US-016 passes: true
- **Migration results:**
  - 124 notes discovered and processed (0 failures, 0 skipped)
  - By type: 8 entity, 39 account, 20 institution, 39 position, 5 vehicle, 12 property, 1 vendor
  - Neo4j: 233 Entity nodes, 34 Bet nodes (from prior tests), 143 Source nodes, 636 relationships
  - Duration: ~15 minutes (sequential cypher-shell calls)
- **Learnings for future iterations:**
  - Properties/Vendors/Telescope Properties LLC.md has `type: entity` (not vendor) — the migration correctly routes it to the entity mapper
  - Finance/Cash Flows/ has only a README.md — no standalone cash flow notes exist; investment cash flows are embedded in Deal note markdown tables
  - Finance/Dashboards/ contains dashboard/MOC type notes — not schema-driven, correctly skipped
  - Vault paths with spaces break shell variable interpolation — always use double-quoted full paths or escaped spaces, never `$VAULT` string vars without quotes
  - Each mapper result type uses a different ID property name (entityId, accountId, cashFlowId, betId, positionId, personId) — check the interface before accessing
  - CashFlowMapperResult has no `openQuestionCreated` field — handle explicitly as false in migration routing
  - `replace_all` in Edit tool can create self-referencing declarations — be careful with variable names that overlap with their initial values
  - Neo4j cypher-shell count output includes column header "c" on separate line — parse carefully in verification queries
  - Migration is idempotent — running twice produces same node counts (MERGE on deterministic IDs)
  - 182 tests total (24 new + 158 existing), all passing
---

## 2026-03-02 - US-017
- **What was implemented:** Atomic claim decomposition engine that takes unstructured note body text, chunks it (2000-token sliding window with 200-token overlap), sends each chunk to an LLM (Ollama qwen3:32b locally, configurable for xAI Grok-4.20) for atomic claim extraction, deduplicates claims via vector similarity (>0.95 cosine = duplicate), stores claims as (:Claim) nodes in Neo4j with embeddings, entity extraction ((:Claim)-[:ABOUT]->(:Entity)), and source linking ((:Claim)-[:SOURCED_FROM]->(:Source)).
- **Files changed:**
  - `src/claim-decomposition.ts` — new decomposition engine: decomposeNote, decomposeNoteFile, decomposeNoteString, chunkText, decomposeChunk, parseLLMResponse, cosineSimilarity, generateClaimId, deduplicateClaims. Configurable LLM backend (Ollama default, xAI via env var). Retry logic (2 retries, 10-min timeout). /no_think directive for qwen3.
  - `tests/claim-decomposition.test.ts` — 36 tests (27 unit + 9 integration): chunking, LLM response parsing, cosine similarity, claim ID generation, LLM config resolution, decompose chunk, deduplication, full decompose-to-Neo4j pipeline, idempotency
  - `scripts/test-claim-decomposition.ts` — integration test script: processes 3 real vault notes (meeting note, capital structure discussion, trip summary), verifies claims in Neo4j
  - `package.json` — added `test-claim-decomposition` npm script
  - `AGENTS.md` — added US-017 to completed stories
  - `prd.json` — US-017 passes: true
- **Integration test results:**
  - Meeting Note (Jacek/Arelion, 2388 chars): 14 claims, 16 entities
  - Capital Structure Discussion (DataBank/Raul, 8040 chars): 30 claims, 22 entities
  - Trip Summary (MetroConnect 2026, 5219 chars): 21 claims, 38 entities
  - Total: 65 claims stored, 76 entities linked, 3 source nodes
- **Learnings for future iterations:**
  - xAI API key is in IronClaw's encrypted keychain, NOT in shell env — decomposition engine defaults to Ollama (local) and can switch to xAI when XAI_API_KEY is set
  - qwen3:32b is slow (~200-400s per 2K-token chunk on M1 Max) — adding `/no_think` to the system prompt disables thinking mode for faster JSON output
  - Chunk threshold matters for local models: 4000 tokens (PRD spec) is too large for qwen3:32b — lowered to 2500 tokens to avoid timeouts
  - `AbortSignal.timeout()` works with `fetch()` for request timeouts — set to 10 minutes for local LLM
  - LLM response parsing must handle: markdown code fences (```json...```), qwen3 `<think>` tags, surrounding narrative text, missing optional fields
  - `require()` doesn't work in ESM modules — use `import` at top level for `execSync` in test availability checks
  - `node:test` `it(name, options, fn)` — options (like `{ timeout: 120_000 }`) go BEFORE the test function, not after
  - Pre-existing test failure in generic-mapper.test.ts (`creates RELATED_TO for loan-account`) — not related to US-017
  - Claims are agent-populated (truth_score: 0.7, truth_basis: "agent-populated") — future stories should allow human verification to upgrade to "verified" (0.95)
  - 218 tests total (36 new + 182 existing), 217 passing (1 pre-existing failure in generic-mapper)
---

# Ralph Progress Log
Started: Tue Mar  3 22:32:44 EST 2026

## Codebase Patterns
- Neo4j ESM import: `import neo4j, { type Driver, type Session } from "neo4j-driver";`
- OpenAI ESM import: `import OpenAI from "openai";` — constructor takes `{ apiKey, baseURL }`
- vi.mock for OpenAI: mock the default export as a class with `embeddings = { create: vi.fn() }`
- Mock call counts accumulate across tests — use `mockFn.mockClear()` before asserting exact call counts
- Neo4j vector search: `CALL db.index.vector.queryNodes('index_name', $topK, $embedding)` returns `node, score`
- Embedding storage: store as `Array.from(float32Array)` since Neo4j doesn't accept Float32Array directly
- Neo4j password from Docker: `NEO4J_AUTH=neo4j/lingelpedia2026` → password is `lingelpedia2026`
- Neo4j container name: `linglepedia` (note the typo — linglepedia vs linglepedia)
- All Cypher DDL uses `IF NOT EXISTS` for idempotency (constraints, range indexes, vector indexes)
- Full-text indexes in Neo4j 5.26 don't support `IF NOT EXISTS` — check existence with `SHOW INDEXES WHERE name = '...'` first
- Neo4j integer results need `.toNumber()` conversion (similar to PostgreSQL BIGINT → Number())
- Tests need `NEO4J_PASSWORD=lingelpedia2026` env var set to connect to local Neo4j
- Knowledge module lives in `src/knowledge/` alongside `src/telemetry/`
- All knowledge operations emit telemetry with `agent_name: "knowledge_agent"` and `event_type: "knowledge_write"` or `"knowledge_query"`
- YAML parsing: `import YAML from "yaml"` then `YAML.parse(content)` — returns plain JS objects, validate with Zod
- Mocking global fetch in vitest: `vi.stubGlobal("fetch", mockFn)` + `vi.unstubAllGlobals()` in cleanup
- Ollama API: `/api/tags` returns `{ models: [{ name: "model:tag" }] }` — use for availability checks
- Frontier-only tasks: claim_decomposition, contradiction_detection, aqm_query, aqm_synthesis, initial_seeding, complex_reasoning
- Local-eligible tasks: formatting, embedding_preprocessing, routine_update, pre_reflection
- Frontmatter regex: `/^---\r?\n([\s\S]*?)\r?\n---\r?\n?([\s\S]*)$/` — captures YAML block and body separately
- VaultConnector write restriction: only `_agent_insights/` directory is writable — enforce with path.resolve + startsWith check
- Mock icloud-sync in vault tests: `vi.mock("../telemetry/icloud-sync.js")` to avoid needing `brctl` binary
- fs.watch with `{ recursive: true }` works on macOS for recursive directory monitoring
- File watcher tests are timing-sensitive — use delays and filter for specific events to avoid catching events from prior tests

---

## 2026-03-03 22:37 EST - US-500
- Implemented Neo4j Docker connection module and schema application
- Files created:
  - `src/knowledge/neo4j.ts` — Neo4jConnection class with configurable bolt URI, auth, pooling, health check
  - `src/knowledge/schema.ts` — applySchema() and verifySchema() functions creating 5 constraints, 1 vector index, 1 fulltext index, 8 lookup indexes
  - `src/knowledge/neo4j.test.ts` — 8 integration tests (connection, health check, schema apply, idempotency, verification, telemetry)
- **Learnings for future iterations:**
  - Neo4j container password is `lingelpedia2026` (set via `NEO4J_AUTH` Docker env var)
  - The neo4j-driver npm package uses ESM default export: `import neo4j from "neo4j-driver"`
  - Neo4j integer types (like count results) require `.toNumber()` — use optional chaining `?.toNumber?.() ?? 0`
  - Full-text indexes don't support `IF NOT EXISTS` in Neo4j 5.26-community — need to check `SHOW INDEXES WHERE name = '...'` first
  - Vector index syntax: `CREATE VECTOR INDEX ... FOR (c:Claim) ON (c.embedding) OPTIONS {indexConfig: {...}}`
  - All 165 tests pass (157 existing v3.3 + 8 new knowledge tests)
---

## 2026-03-03 22:41 EST - US-501
- Implemented OpenAI embedding pipeline with Neo4j vector storage
- Files created:
  - `src/knowledge/embedding.ts` — EmbeddingPipeline class with embed(), embedBatch(), storeEmbedding(), embedAndStore(), reembed(), semanticSearch()
  - `src/knowledge/embedding.test.ts` — 12 tests (single embed, batch, storage, vector search, graceful degradation, retry backoff, re-embed, cost telemetry)
- **Learnings for future iterations:**
  - OpenAI SDK v6 default export: `import OpenAI from "openai"` with `new OpenAI({ apiKey, baseURL })`
  - `client.embeddings.create({ model, input })` — input can be a string array for batch; response has `.data[].embedding` and `.usage.total_tokens`
  - Neo4j vector search uses `CALL db.index.vector.queryNodes('index_name', topK, embeddingArray)` — returns `node` and `score`
  - Must convert Float32Array to plain Array via `Array.from()` before passing to Neo4j Cypher params
  - vi.mock mock call counts accumulate across tests — always `mockFn.mockClear()` before asserting exact call counts
  - Non-blocking pattern: try embedding, catch errors, flag claim with `embedding_pending: true` and `embedding_error` — never throw
  - All 177 tests pass (165 existing + 12 new embedding tests)
---

## 2026-03-03 22:47 EST - US-502
- Implemented Knowledge Agent identity file parser and model router
- Files created:
  - `src/knowledge/identity.ts` — YAML identity file parser with Zod schema validation for all identity fields (name, role, personality, ownership, data_access, privacy_duties, model_intelligence, thinking_model, north_star, trust_level, operating_algorithm, charter_principles, deletion_policy, tools)
  - `src/knowledge/model-router.ts` — ModelRouter class routing tasks to frontier (grok-4-1-fast-reasoning), local (qwen3:32b via Ollama), or embeddings (text-embedding-3-small) based on task type classification
  - `src/knowledge/identity.test.ts` — 14 tests (file parsing, typed config, perspectives, data access, validation errors)
  - `src/knowledge/model-router.test.ts` — 18 tests (frontier routing for 6 task types, local routing for 4 task types, Ollama fallback, embedding routing, telemetry)
  - `src/knowledge/fixtures/lingelpedia.yaml` — Knowledge Agent identity YAML fixture from Implementation Guide spec
- Installed `yaml` npm package for YAML parsing
- **Learnings for future iterations:**
  - `yaml` npm package: `import YAML from "yaml"` then `YAML.parse(content)` — returns plain JS objects
  - ModelRouter uses `fetch()` to check Ollama availability at `/api/tags` — returns `{ models: [{ name: "..." }] }`
  - Ollama availability check has 2-second timeout via AbortController to avoid blocking
  - Frontier-only tasks (AQM, decomposition, contradiction, seeding, complex reasoning) are NEVER delegated to local
  - Local-eligible tasks (formatting, preprocessing, routine updates, pre-reflections) fall back to frontier with `cost_warning: true` when Ollama is unavailable
  - vi.stubGlobal("fetch", mockFn) and vi.unstubAllGlobals() for mocking global fetch in vitest
  - Router config defaults: default_model=frontier, cost_optimization=true, supervision_sample_rate=0.1, quality_threshold=0.85
  - All 209 tests pass (177 existing + 32 new identity/router tests)
---

## 2026-03-03 22:52 EST - US-503
- Implemented Obsidian vault connection with iCloud sync integration
- Files created:
  - `src/knowledge/vault-connector.ts` — VaultConnector class with scan(), writeInsight(), write(), startWatching(), stopWatching()
  - `src/knowledge/vault-connector.test.ts` — 23 tests (frontmatter parsing, body extraction, vault scanning, glob filtering, iCloud materialization, write access restriction, path traversal prevention, file watcher, telemetry emission, edge cases)
- Key features:
  - Integrates `materialize_icloud_stubs()` from v3.3 before every vault scan
  - Recursive .md file discovery, skipping hidden directories (.obsidian, .trash)
  - YAML frontmatter parsing with round-trip serialization via `serializeNote()`
  - Write access restricted to `_agent_insights/` only — path traversal attacks blocked via `path.resolve()` + `startsWith()` check
  - Selective scan with glob patterns (e.g., `Finance/Entities/*.md`)
  - File watcher using `fs.watch({ recursive: true })` with event handler pattern
  - All operations emit telemetry with `agent_name: "knowledge_agent"`
- **Learnings for future iterations:**
  - Frontmatter regex `/^---\r?\n([\s\S]*?)\r?\n---\r?\n?([\s\S]*)$/` reliably separates YAML from body
  - `YAML.parse()` may return non-object for invalid frontmatter — always check `typeof parsed === "object"`
  - `path.resolve()` + `startsWith()` is the pattern for preventing path traversal in write restrictions
  - fs.watch events from prior tests can bleed into watcher tests — drain events with a delay before writing test files
  - Mock `icloud-sync.js` entirely in vault tests to avoid `brctl` dependency
  - All 232 tests pass (209 existing + 23 new vault-connector tests)
---

# Ralph Progress Log
Started: Sun Mar  1 23:44:47 EST 2026

## Codebase Patterns
- TypeScript project: use `npm run typecheck` (tsc --noEmit) before committing
- Run scripts with `npx tsx scripts/<name>.ts`
- IronClaw CLI: `ironclaw status` for health, `ironclaw config list` for settings, `ironclaw doctor` for diagnostics
- IronClaw stores secrets in macOS keychain (`secrets_master_key_source: keychain`), not in env vars
- IronClaw config is in `~/.ironclaw/.env` (database-backed, use `ironclaw config set` to modify)
- PostgreSQL for IronClaw runs via Homebrew on /tmp:5432 (not Docker)
- IronClaw MCP servers managed via `ironclaw mcp list` / `ironclaw mcp add`
- Always update AGENTS.md Environment Details table when infrastructure changes
- Docker Desktop settings at `~/Library/Group Containers/group.com.docker/settings.json`
- pmset changes require sudo — scripts that need it should document the sudo requirement clearly
- Docker `--restart unless-stopped` is a convention enforced in all `docker run` commands (not automated tooling)

---

## 2026-03-02 - US-001
- **What was implemented:** Environment health-check diagnostic script + AGENTS.md environment verification
- **Files changed:**
  - `scripts/health-check.ts` — new diagnostic script checking Docker, IronClaw, PostgreSQL, xAI API, pmset
  - `package.json` — new TypeScript project setup with tsx, tsc
  - `tsconfig.json` — strict TypeScript config
  - `.gitignore` — added node_modules/ and dist/
  - `AGENTS.md` — updated Environment Details with verified component statuses and host details
  - `prd.json` — US-001 passes: true
- **Learnings for future iterations:**
  - IronClaw default model was `grok-4-1-fast-reasoning`, updated to `grok-4.20` per PRD via `ironclaw config set selected_model grok-4.20`
  - xAI API key is NOT in shell environment — it's in IronClaw's keychain. Direct curl tests need the key exported; IronClaw handles auth internally.
  - `ironclaw -m "..."` single-message mode exists but can be slow/hang — use `ironclaw doctor` for quick connectivity checks instead
  - pmset sleep is currently not disabled (sleep=1, disksleep=10 on AC) — US-002 will fix this
  - IronClaw has 1 MCP server configured (imessage on port 8892) — Neo4j, filesystem, Google APIs still to be added
  - PostgreSQL is Homebrew-managed, not Docker — important distinction for container management stories
---

## 2026-03-02 - US-002
- **What was implemented:** Always-on configuration script + Docker autoStart + restart policy convention
- **Files changed:**
  - `scripts/configure-always-on.ts` — new script that configures pmset (sleep=0, disksleep=0), Docker autoStart, and documents restart policy
  - `package.json` — added `configure-always-on` npm script
  - `AGENTS.md` — added Always-On Configuration section, updated host details, added to completed stories
  - `prd.json` — US-002 passes: true
- **Learnings for future iterations:**
  - Docker Desktop settings are in `~/Library/Group Containers/group.com.docker/settings.json` — can be modified directly as JSON
  - `autoStart: true` in Docker settings makes Docker start on login (no launchd plist needed for Docker Desktop)
  - pmset requires sudo — can't be applied from non-interactive sessions like Claude Code. Document sudo commands clearly.
  - Docker has launchd plists at `/Library/LaunchDaemons/com.docker.{socket,vmnetd}.plist` for its daemon infrastructure
  - The `--restart unless-stopped` policy is a convention, not an automated check — each `docker run` in subsequent stories must include it
---

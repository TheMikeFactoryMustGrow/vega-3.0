# Ralph Progress Log
Started: Tue Mar  3 16:26:56 EST 2026

## Codebase Patterns
- TypeScript project uses ESM (`"type": "module"`) with `moduleResolution: "bundler"` in tsconfig
- Use Zod for runtime schema validation of all data structures (events, frontmatter, etc.)
- Telemetry modules live in `src/telemetry/` — each concern in its own file (types.ts, emitter.ts, rotation.ts)
- Non-blocking pattern: wrap all I/O in try/catch, return null on failure, log to stderr
- File paths use `{YYYY-MM-DD}.jsonl` naming convention (no "events-" prefix)
- Tests use vitest with temp directories (`mkdtemp`) for filesystem isolation
- All exports go through `index.ts` barrel files
- Base telemetry path: `~/vega-telemetry/` with subdirs `events/`, `archive/`
- PostgreSQL `BIGINT` columns return strings in node-pg — always use `Number()` coercion in parse functions
- Use `pg` (node-postgres) with default import: `import pg from "pg"; const { Pool } = pg;` (ESM compat)
- Tier 2 tables use `ON CONFLICT DO UPDATE` for idempotent upserts — conflict keys are the UNIQUE constraints
- Database connection: `VEGA_PG_URL` env var for prod, `VEGA_TEST_PG_URL` for tests (default: `postgres://localhost/vega_test`)
- Tier 2 types in `tier2-types.ts`, migration DDL in `database.ts`, CRUD in `tier2-repository.ts`
- Aggregation jobs in `aggregation.ts` — use `{ emitTelemetry: false }` option in tests to avoid side effects
- vitest `fileParallelism: false` required because test files share PostgreSQL database state
- Anomaly detection needs variance in historical data (stddev > 0) to trigger — uniform values produce stddev=0, skipping detection
- Cost estimation: `estimateCost()` uses per-model rates ($/Mtok); local models like `qwen3:32b` cost $0
- `emitter.readEvents(date)` returns events for one day; for hourly aggregation, filter by timestamp range
- Morning Brief module uses raw pool queries (not Tier2Repository) for analytical cross-agent aggregations
- `beforeEach` with DELETE FROM statements cleans table data between tests while preserving schema
- Raw SQL in metrics_query: avoid hardcoded date literals like `date = '2026-03-02'` — pg serializes JS Date objects using local timezone, causing mismatches. Filter by agent_name only or use parameterized queries
- Self-assessment types in `self-assessment-types.ts`, runner in `self-assessment-runner.ts` — use `{ emitTelemetry: false }` in tests
- Weekly reflections in `weekly-reflection.ts` + `weekly-reflection-types.ts` — generator writes files to `~/vega-telemetry/reflections/{agent_name}/`
- Contradiction detection compares reflection content (what_worked_well/what_didnt_work) against actual Tier 2 quality_daily trends — data-driven reflections won't say "no issues" when anomalies exist, so test for "claims positive + declining trends" instead
- Cross-agent patterns require ≥2 agents; complementary pattern checks success_rate gap (≥0.9 vs <0.7)
- Monthly reviews in `monthly-review.ts` + `monthly-review-types.ts` — Bet nodes stored in PostgreSQL `telemetry_bets` table (pending Neo4j/Lingelpedia integration)
- Bet lifecycle: pending_approval → active (Mike approves) → confirmed/revised/abandoned (assessed at next monthly review)
- `dropTier2Tables()` includes `telemetry_bets` — all test files sharing DB must account for this table
- Bar Raiser monitor in `bar-raiser-monitor.ts` + types in `bar-raiser-monitor-types.ts` — uses `{ emitTelemetry: false }` in tests
- Monitor detection patterns use period-over-period comparison (prior 7 days vs current 7 days) for metric gaming
- Scope creep detection reads reflection files from `~/vega-telemetry/reflections/{agent_name}/` — same pattern as weekly reflections
- ≥3 authority keywords = critical severity; fewer = warning. Configurable via MonitorConfig
- iCloud stubs follow naming: `.filename.ext.icloud` → materialized as `filename.ext` (leading dot + trailing .icloud)
- Use `exec_fn` dependency injection for testing shell commands like `brctl download` — avoids needing actual iCloud environment

---

## 2026-03-03T16:33 EST - US-400
- **What was implemented**: Tier 1 telemetry event stream (JSONL) — the foundational telemetry layer
  - `TelemetryEmitter` class: accepts structured event data, validates with Zod, appends to daily JSONL files
  - Event schema with all required fields: event_id (UUID), timestamp (ISO 8601), agent_name, event_type, event_subtype, model_used, tokens_in, tokens_out, latency_ms, outcome, metadata (flexible JSON), session_id
  - All 7 event_type categories: agent_action, model_call, knowledge_write, knowledge_query, escalation, schedule_trigger, system_event
  - Rotation job: archives files >30 days (gzip), deletes archives >90 days
  - Non-blocking emission: failures return null, never throw
  - 12 tests covering all acceptance criteria
- **Files changed**:
  - `package.json` (NEW) — project setup with TypeScript, vitest, zod
  - `tsconfig.json` (NEW) — TypeScript config targeting ES2022/ESM
  - `vitest.config.ts` (NEW) — test runner config
  - `src/telemetry/types.ts` (NEW) — Zod schema + TypeScript types for telemetry events
  - `src/telemetry/emitter.ts` (NEW) — TelemetryEmitter class with emit/emitBatch/readEvents
  - `src/telemetry/rotation.ts` (NEW) — runRotation() for daily file rotation
  - `src/telemetry/index.ts` (NEW) — barrel exports
  - `src/telemetry/telemetry.test.ts` (NEW) — 12 tests covering schema, emission, rotation
- **Learnings for future iterations:**
  - The PRD field names differ slightly from the Implementation Guide (e.g., PRD uses `agent_name`, guide uses `agent`; PRD uses `model_used`, guide uses `details.model`). PRD acceptance criteria are the authoritative source for field naming.
  - The PRD lists 7 event_type categories while the Implementation Guide lists 8 with different names. Follow PRD for acceptance criteria.
  - Zod `.datetime({ offset: true })` is needed for ISO 8601 timestamps with timezone offsets
  - `writeFile` for first write, `appendFile` for subsequent — avoids empty file check race condition
  - Rotation job uses atomic gzip (write to .tmp then rename) to prevent partial archives
---

## 2026-03-03T16:41 EST - US-401
- **What was implemented**: Tier 2 PostgreSQL aggregation tables — four queryable time-series tables
  - `telemetry_agent_hourly`: per-agent hourly aggregates with action_count, latency metrics (avg, p95, p99), reasoning_steps_avg, model_distribution (JSONB), tokens, cost, performance_score. UNIQUE(agent_name, hour_bucket)
  - `telemetry_cost_daily`: per-agent per-model daily cost breakdown with total_tokens_in/out, total_cost_usd, avg_cost_per_action, cost_per_mtok. UNIQUE(date, agent_name, model)
  - `telemetry_quality_daily`: per-agent per-metric daily quality tracking with metric_value, trend (improving/stable/declining), p50/p95 values, sample_count. UNIQUE(date, agent_name, metric_name)
  - `telemetry_anomalies`: event-driven anomaly records with severity (info/warning/critical), detection_method, anomaly_details (JSONB), acknowledged/resolved lifecycle
  - All tables have indexes per Implementation Guide spec
  - Retention cleanup function: `telemetry_cleanup_old_data(retention_days)` deletes data older than threshold
  - Tier2Repository class with full upsert/query operations for all 4 tables
  - Zod schemas for all table row types
  - 14 tests covering INSERT→SELECT, ON CONFLICT upsert idempotency, anomaly lifecycle, schema validation, migration idempotency
- **Files changed**:
  - `package.json` (MODIFIED) — added `pg` dependency and `@types/pg` devDependency
  - `src/telemetry/tier2-types.ts` (NEW) — Zod schemas and TS types for all 4 Tier 2 tables
  - `src/telemetry/database.ts` (NEW) — PostgreSQL pool creation, migration DDL, table cleanup
  - `src/telemetry/tier2-repository.ts` (NEW) — Tier2Repository class with upsert/query ops
  - `src/telemetry/tier2.test.ts` (NEW) — 14 integration tests against PostgreSQL
  - `src/telemetry/index.ts` (MODIFIED) — added Tier 2 barrel exports
  - `prd.json` (MODIFIED) — US-401 passes: true
- **Learnings for future iterations:**
  - PostgreSQL `BIGINT` columns are returned as strings by node-pg. Must use `Number()` coercion in row parsers for tokens_in_total, tokens_out_total, and any BIGINT field.
  - PostgreSQL `DECIMAL`/`NUMERIC` columns also return strings — same coercion needed for cost_usd fields.
  - `import pg from "pg"; const { Pool } = pg;` is the ESM-compatible way to import node-postgres.
  - PRD acceptance criteria field names (hour_bucket, action_count, performance_score) sometimes differ from Implementation Guide names (hour_start, tool_calls_total). PRD is authoritative.
  - Use `z.coerce.date()` in Zod schemas for columns that could be Date objects OR date strings from PostgreSQL.
  - Idempotent migrations: use `IF NOT EXISTS` for tables/indexes and `CREATE OR REPLACE` for functions.
  - Test database is `vega_test` (already exists on this machine).
---

## 2026-03-03T16:50 EST - US-402
- **What was implemented**: Aggregation jobs (hourly and daily) — the bridge from Tier 1 JSONL to Tier 2 PostgreSQL
  - `runHourlyAggregation()`: reads JSONL events for a specific hour, groups by agent, computes aggregates (action_count, success/error counts, avg/p95/p99 latency, model_distribution, tokens, cost, performance_score), upserts into `telemetry_agent_hourly`
  - `runDailyAggregation()`: reads JSONL events for a full day, computes cost_daily rollups (per agent+model), quality_daily metrics (success_rate, avg_latency_ms, error_rate with trend), and anomaly detection (>2σ deviation from 7-day rolling average on error_count and latency)
  - Both jobs are idempotent via ON CONFLICT DO UPDATE
  - Both jobs log execution to Tier 1 event stream (event_type: system_event, event_subtype: aggregation_complete)
  - `backfillHourly()` and `backfillDaily()`: iterate over time ranges to re-aggregate historical data
  - Cost estimation via per-model rate table (frontier models: $3/$15 per Mtok in/out, local models: $0)
  - Performance score: weighted composite (0.7 * success_rate + 0.3 * latency_efficiency)
  - Trend computation: compares today vs yesterday, >5% delta = improving/declining
  - 14 tests covering all acceptance criteria
- **Files changed**:
  - `src/telemetry/aggregation.ts` (NEW) — hourly + daily aggregation jobs, backfill, anomaly detection
  - `src/telemetry/aggregation.test.ts` (NEW) — 14 integration tests
  - `src/telemetry/index.ts` (MODIFIED) — added aggregation barrel exports
  - `vitest.config.ts` (MODIFIED) — added `fileParallelism: false` for shared DB tests
  - `prd.json` (MODIFIED) — US-402 passes: true
- **Learnings for future iterations:**
  - Tests sharing PostgreSQL must run sequentially (`fileParallelism: false`) — parallel test files doing `dropTier2Tables` + `runTier2Migration` cause race conditions on `pg_type` unique constraints.
  - Anomaly detection with >2σ needs non-zero stddev in historical data. Uniform historical values (all same) produce stddev=0 and skip detection. Tests must include variance.
  - `emitter.readEvents(date)` reads entire day — for hourly aggregation, filter events by timestamp range client-side.
  - Cost estimation is model-aware: `qwen3:32b` (local) = $0, `grok-4-1-fast-reasoning` = $3/$15 per Mtok. Events with null `model_used` get $0 cost.
  - The `_pool` parameter in `runDailyAggregation` is currently unused (anomaly detection queries through `repo` instead) but kept for API consistency and future use.
---

## 2026-03-03T16:55 EST - US-403
- **What was implemented**: Morning Brief System Health section — the user-facing telemetry output
  - `MorningBriefHealth` class with `generate(targetDate)` method returning structured markdown
  - 6 subsections generated via dedicated SQL queries against Tier 2 tables:
    1. **Active Agents (24h)**: action counts per agent from telemetry_agent_hourly
    2. **Total Actions**: sum with day-over-day percentage delta
    3. **Estimated Cost**: per agent+model breakdown with daily total from telemetry_cost_daily
    4. **Quality Scores**: per-agent metrics with trend arrows (↑/↓/→) from telemetry_quality_daily
    5. **Anomalies**: unresolved count with severity breakdown from telemetry_anomalies
    6. **Top Performers**: ranked by avg performance_score from telemetry_agent_hourly
  - Trend arrows: ↑ for >5% improvement, ↓ for >5% decline, → for stable
  - Graceful fallback: "Telemetry data collecting — system health available tomorrow" when Tier 2 tables empty
  - Valid Obsidian-compatible markdown output
  - 3 tests covering: empty table fallback, full 6-subsection generation with value verification, markdown validity
- **Files changed**:
  - `src/telemetry/morning-brief-health.ts` (NEW) — MorningBriefHealth class with 6 SQL-backed subsections
  - `src/telemetry/morning-brief-health.test.ts` (NEW) — 3 integration tests
  - `src/telemetry/index.ts` (MODIFIED) — added Morning Brief barrel exports
  - `prd.json` (MODIFIED) — US-403 passes: true
- **Learnings for future iterations:**
  - Morning Brief subsections use raw pool queries rather than Tier2Repository, because they need cross-agent analytical aggregations (GROUP BY agent_name, SUM, AVG) that don't fit the CRUD-style repo pattern.
  - Use `beforeEach` with `DELETE FROM` statements (not dropTier2Tables/runTier2Migration) to clean data between tests — much faster and avoids DDL race conditions.
  - Day-over-day delta computation requires querying the previous day's data too — the hasDataForDate check only looks at the target day's telemetry_agent_hourly rows.
  - PostgreSQL `COALESCE(SUM(...), 0)` returns a string for BIGINT aggregates — use `Number()` coercion.
  - Trend arrows map: "improving" → ↑, "declining" → ↓, "stable"/null → →. The 5% threshold matches the aggregation job's trend computation.
---

## 2026-03-03T17:03 EST - US-404
- **What was implemented**: Loop 1: Operational Learning (self_assessment YAML) — the fastest feedback loop
  - `SelfAssessmentConfigSchema`: Zod schema for the self_assessment YAML block in agent identity files, including frequency, metrics_query (SQL), adjustment_rules, and reasoning_prompt_injection
  - Three condition types for adjustment rules: `metric_value` (numeric comparisons: <, >, =, <=, >=), `trend` (declining/stable/improving), and `threshold` (min/max bounds)
  - `SelfAssessmentRunner` class: reads self_assessment config, executes metrics_query against telemetry_quality_daily, evaluates adjustment_rules, updates reasoning_prompt_injection
  - Bar Raiser override mechanism: `addOverride(agent, rule)` / `removeOverride(agent, rule)` — overridden rules are skipped even when condition is met
  - All adjustments logged to Tier 1 telemetry (event_type: system_event, event_subtype: self_assessment) with before/after prompt values
  - Override events also logged (event_subtype: self_assessment_override) for the Bar Raiser agent
  - 11 tests covering: metric_value triggers, non-triggers, telemetry logging with before/after, trend conditions, threshold conditions, Bar Raiser override/removal, multiple rules, and empty query results
- **Files changed**:
  - `src/telemetry/self-assessment-types.ts` (NEW) — Zod schemas for SelfAssessmentConfig, AdjustmentRule, RuleCondition (3 variants), and result types
  - `src/telemetry/self-assessment-runner.ts` (NEW) — SelfAssessmentRunner class with condition evaluation, override support, and telemetry logging
  - `src/telemetry/self-assessment.test.ts` (NEW) — 11 integration tests against PostgreSQL
  - `src/telemetry/index.ts` (MODIFIED) — added Loop 1 barrel exports
  - `prd.json` (MODIFIED) — US-404 passes: true
- **Learnings for future iterations:**
  - Raw SQL in metrics_query must not use hardcoded date literals like `date = '2026-03-02'` — node-pg serializes JS Date objects using the local timezone, so a Date at UTC midnight may become the previous day's date in EST. Use agent_name-only filters or parameterized queries.
  - The self_assessment YAML block is a config structure, not an actual YAML file — since agent identity files (US-209) are planned but not yet created as real files, the schema defines the TypeScript types that will map to the YAML block.
  - `evaluateCondition` uses `rows.some()` — a condition fires if ANY row matches, which is correct for multi-metric agents (e.g., success_rate AND avg_latency_ms).
  - Actions are deliberately limited to `update_prompt` — the PRD constrains agents to only tuning their reasoning prompts, not changing capabilities or trust levels.
---

## 2026-03-03T17:10 EST - US-405
- **What was implemented**: Loop 2: Pattern Learning (weekly reflections) — the pattern recognition cycle
  - `WeeklyReflectionGenerator` class with two main methods:
    - `generateReflection(agentName, weekEndDate)`: generates a single agent's weekly reflection with all 5 required sections (what worked well, what didn't, patterns noticed, proposed adjustments, questions for Bar Raiser)
    - `generateSynthesis(agentNames, weekEndDate)`: generates Bar Raiser cross-agent synthesis with pattern detection and contradiction flagging
  - Reflection sections generated from Tier 2 SQL queries: telemetry_agent_hourly (weekly summary), telemetry_quality_daily (trend analysis), telemetry_anomalies (issue detection)
  - Cross-agent pattern detection: shared_improvement, shared_degradation, complementary (success rate gap), contradiction
  - Contradiction detection: compares reflection self-assessment claims against actual quality_daily trends; flags when agent claims positive but metrics show declining
  - Bar Raiser synthesis includes: overall system health assessment, agent summaries, cross-agent patterns, contradictions, recommendations
  - Reflections written to: `~/vega-telemetry/reflections/{agent_name}/{YYYY-MM-DD}.md`
  - Bar Raiser synthesis written to: `~/vega-telemetry/reflections/bar_raiser/synthesis_{YYYY-MM-DD}.md`
  - All reflection/synthesis events logged to Tier 1 telemetry (event_type: system_event, event_subtype: weekly_reflection / weekly_synthesis)
  - 4 tests covering: full reflection with all sections, empty data graceful handling, cross-agent synthesis with pattern detection, contradiction detection
- **Files changed**:
  - `src/telemetry/weekly-reflection-types.ts` (NEW) — Zod schemas for WeeklyReflection, CrossAgentPattern, BarRaiserSynthesis
  - `src/telemetry/weekly-reflection.ts` (NEW) — WeeklyReflectionGenerator class with reflection generation, cross-agent analysis, contradiction detection
  - `src/telemetry/weekly-reflection.test.ts` (NEW) — 4 integration tests against PostgreSQL
  - `src/telemetry/index.ts` (MODIFIED) — added Loop 2 barrel exports
  - `prd.json` (MODIFIED) — US-405 passes: true
- **Learnings for future iterations:**
  - Data-driven reflections from deterministic code won't produce "no issues" when anomalies exist (the reflection itself surfaces them). Contradiction detection must focus on cross-source comparison: self-assessment claims (what_worked_well) vs. actual Tier 2 trends.
  - Cross-agent pattern detection needs ≥2 agents to function. Complementary pattern checks for success rate gap ≥0.9 vs <0.7.
  - Weekly reflections use raw pool queries (same pattern as Morning Brief) for cross-table analytical aggregations — Tier2Repository is for CRUD operations.
  - Reflection file storage follows the telemetry directory structure: `{basePath}/reflections/{agent_name}/{YYYY-MM-DD}.md`.
---

## 2026-03-03T17:17 EST - US-406
- **What was implemented**: Loop 3: Structural Learning (monthly reviews with Bet tracking) — the highest-stakes learning cycle
  - `MonthlyReviewGenerator` class with monthly review generation and full Bet lifecycle management
  - Monthly reviews synthesize: agent telemetry trends (from Tier 2), Bar Raiser weekly synthesis observations (from filesystem), previous month's active Bet outcomes, and new structural proposals
  - Structural proposals generated automatically based on: declining performance trends, high anomaly counts (≥5), low success rates (<70%)
  - Each proposal follows the Bet format: hypothesis, expected_outcome, measurement_criteria, rollback_trigger, rationale
  - Proposals stored as `pending_approval` Bets — only Mike can approve (transitions to `active`)
  - Bet outcome assessment at monthly review: evaluates active Bets against current telemetry → confirmed (strong performance), revised (moderate), or abandoned (poor)
  - `telemetry_bets` PostgreSQL table with full lifecycle: pending_approval → active → confirmed/revised/abandoned
  - Bet CRUD operations: createBet, approveBet, updateBetOutcome, queryBetsByStatus/Agent/Month
  - Monthly trend computation: first-half vs second-half performance comparison for trend direction
  - Bar Raiser observation extraction: reads Recommendations sections from weekly synthesis markdown files
  - Review markdown output with 5 sections: Agent Performance Trends, Bar Raiser Observations, Previous Bet Outcomes, Structural Proposals, Proposals Awaiting Review
  - Reviews stored at: `~/vega-telemetry/reviews/{YYYY-MM}.md`
  - 7 tests covering: full review generation with all sections, empty data graceful handling, Bet create→approve→confirm lifecycle, Bet abandoned on poor performance, Bet revised on moderate performance, proposals stored as pending_approval, unapproved bets not assessed
- **Files changed**:
  - `src/telemetry/monthly-review-types.ts` (NEW) — Zod schemas for Bet, BetInput, StructuralProposal, BetOutcome, MonthlyTrendSummary, MonthlyReview
  - `src/telemetry/monthly-review.ts` (NEW) — MonthlyReviewGenerator class with review generation, Bet lifecycle, trend computation
  - `src/telemetry/monthly-review.test.ts` (NEW) — 7 integration tests against PostgreSQL
  - `src/telemetry/database.ts` (MODIFIED) — added telemetry_bets table DDL and indexes, updated dropTier2Tables
  - `src/telemetry/index.ts` (MODIFIED) — added Loop 3 barrel exports
  - `prd.json` (MODIFIED) — US-406 passes: true
- **Learnings for future iterations:**
  - Bets are stored in PostgreSQL (not Neo4j/Lingelpedia yet) because the Neo4j driver isn't integrated. When Lingelpedia is available, Bet nodes should be dual-written to both PostgreSQL and Neo4j.
  - Bet assessment uses simple performance thresholds (perf ≥0.8 + success ≥85% = confirmed, perf ≥0.6 + success ≥70% = revised, else abandoned). These should be tunable per agent in the future.
  - Bar Raiser observations are extracted by reading the "## Recommendations" section from weekly synthesis markdown files — format-dependent parsing.
  - Monthly trend direction compares first-half vs second-half of the month using AVG(performance_score) with a 5% delta threshold (same as daily trend computation).
  - The `source_review_month` field (YYYY-MM format) links Bets back to their originating monthly review for traceability.
---

## 2026-03-03T17:23 EST - US-407
- **What was implemented**: Bar Raiser Learning Monitors — 3 detection patterns for pathological self-improvement
  - `BarRaiserMonitor` class with `runMonitor(agentNames, targetDate)` method
  - **Pattern 1 — Metric Gaming**: 3 subtypes
    - Sandbagging: accuracy up + volume down (cherry-picking easy tasks)
    - Shortcutting: latency down + quality down (cutting corners for speed)
    - Avoidance: escalation rate rising (avoiding tasks through escalation)
  - **Pattern 2 — Scope Creep**: 3 subtypes
    - Authority expansion: detects "I should also handle", "I could take over", etc. in reflections
    - Domain expansion: detects "outside my usual domain", "extending into", etc.
    - Trust creep: detects "increase my trust", "more autonomy", "less oversight" language
  - **Pattern 3 — Confirmation Bias**: 3 subtypes
    - No-issues streak: N consecutive weekly reflections reporting no issues (default: 3)
    - Sentiment-metric mismatch: positive reflection sentiment but declining quality_daily trends
    - Unchanged assessment: identical "Proposed Adjustments" sections across N consecutive weeks
  - All detections logged to `telemetry_anomalies` with `detection_method='bar_raiser_monitor'`
  - Configurable thresholds via `MonitorConfig` (all with sensible defaults)
  - Daily monitoring report written to `~/vega-telemetry/monitor-reports/{YYYY-MM-DD}.md`
  - Critical detections (≥3 authority expansion keywords) set `has_critical` flag for Morning Brief alert
  - 9 tests covering: all 3 metric gaming subtypes, scope creep authority expansion, confirmation bias no-issues streak, sentiment-metric mismatch, clean report generation, critical escalation flag, no false positives for stable agents
- **Files changed**:
  - `src/telemetry/bar-raiser-monitor-types.ts` (NEW) — Zod schemas for Detection, MonitorConfig, MonitorReport, pattern type enums
  - `src/telemetry/bar-raiser-monitor.ts` (NEW) — BarRaiserMonitor class with 3 detection patterns
  - `src/telemetry/bar-raiser-monitor.test.ts` (NEW) — 9 integration tests against PostgreSQL + filesystem
  - `src/telemetry/index.ts` (MODIFIED) — added Bar Raiser Monitor barrel exports
  - `prd.json` (MODIFIED) — US-407 passes: true
- **Learnings for future iterations:**
  - Period-over-period comparison (prior N days vs current N days) is the cleanest approach for detecting metric gaming — avoids needing rolling averages or stddev calculations
  - Scope creep detection via keyword matching in reflection markdown is effective but format-dependent — relies on reflections being stored as readable markdown files at known paths
  - ≥3 authority keyword matches = critical severity; this threshold is configurable but provides good signal-to-noise
  - Confirmation bias detection for "no issues streak" depends on specific phrases like "No significant issues" — must match what the WeeklyReflectionGenerator actually emits in its `what_didnt_work` section
  - Sentiment-metric mismatch checks "What Worked Well" for positive language AND verifies absence of "declining trends" to avoid flagging reflections that DO acknowledge problems
---

## 2026-03-03T17:26 EST - US-408
- **What was implemented**: iCloud sync handler with stub materialization — silent data loss prevention for iCloud Drive users
  - `materialize_icloud_stubs()` function: scans a directory recursively for `.icloud` stub files, invokes `brctl download` for each, waits for materialization with configurable timeout/polling
  - `find_icloud_stubs()`: recursive directory scanner that detects files ending with `.icloud` extension
  - `get_materialized_path()`: converts stub path (`.filename.ext.icloud`) to expected materialized path (`filename.ext`)
  - Returns `MaterializationReport`: total_stubs_found, successfully_materialized, failed (with error details)
  - Graceful degradation: failed materializations logged to stderr as warnings, never block processing
  - Dependency injection via `exec_fn` option for testability — avoids needing actual iCloud environment
  - Configurable: timeout_ms (default 30000), poll_interval_ms (default 1000), brctl_command path
  - 11 tests covering: stub detection (flat + nested + empty + nonexistent dirs), path conversion, successful materialization, timeout handling, brctl command failure, no stubs case, mixed success/failure
- **Files changed**:
  - `src/telemetry/icloud-sync.ts` (NEW) — materialize_icloud_stubs, find_icloud_stubs, get_materialized_path
  - `src/telemetry/icloud-sync.test.ts` (NEW) — 11 tests with mock brctl via exec_fn injection
  - `src/telemetry/index.ts` (MODIFIED) — added iCloud sync barrel exports
  - `prd.json` (MODIFIED) — US-408 passes: true
- **Learnings for future iterations:**
  - iCloud stubs follow the pattern `.filename.ext.icloud` — the leading dot is added by iCloud and `.icloud` is appended. To get the real filename, strip leading dot and trailing `.icloud`
  - `brctl download` is the macOS-specific command to trigger iCloud materialization — only available on macOS with iCloud Drive active
  - Testing shell-dependent features requires dependency injection (`exec_fn` parameter) since the actual `brctl` command won't be available in CI or non-macOS environments
  - Polling for file materialization needs both existence check AND size > 0 — iCloud sometimes creates a 0-byte placeholder before the real content arrives
---
